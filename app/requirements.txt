# StackBridge Orders API — Python dependencies
# All pinned to specific versions.
# Run `pip-audit -r requirements.txt` to check for CVEs before upgrading.

Flask==3.1.3
Jinja2==3.1.6
Werkzeug==3.1.8
psycopg2-binary==2.9.12
requests==2.34.2
PyYAML==6.0.3
gunicorn==26.0.0
prometheus-client==0.21.1
