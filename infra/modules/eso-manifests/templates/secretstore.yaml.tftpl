apiVersion: external-secrets.io/v1
kind: SecretStore

metadata:
  name: ${service_name}-secret-store
  namespace: ${namespace}

spec:
  provider:
    aws:
      service: SecretsManager
      region: ${aws_region}
      auth:
        jwt:
          serviceAccountRef:
            name: ${service_name}-eso-sa
